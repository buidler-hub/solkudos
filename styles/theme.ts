import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
    fonts: {
        body: 'ClashDisplay, serif',
        heading: 'ClashDisplay, serif',
    },
});

export default theme;
